const  capitalizeWords = (array)=>{
    return array.map(capitalize);
}

const inputStrings = ["hello world", "goodbye", "javascript is fun"];
const capitalize = (a)=> {a.toUpperCase(); return 

const ans =  capitalizeWords(inputStrings , capitalize);
console.log(ans);